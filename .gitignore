/target
Cargo.lock
*.rs.bk
*.orig
.DS_Store
.maki
# Machine-specific local dep overrides (brush/pkgcraft worktree paths); the
# committed manifests resolve these from git. Keep these untracked.
**/.cargo/config.toml

# Local cargo patches (sibling brush/pkgcraft worktrees) — never commit
.cargo/
